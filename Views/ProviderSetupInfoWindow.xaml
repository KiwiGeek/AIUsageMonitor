<local:FluentDialogWindow x:Class="AIUsageMonitor.Views.ProviderSetupInfoWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:local="clr-namespace:AIUsageMonitor.Views"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Width="500"
                 Height="240"
                 MinWidth="460"
                 MinHeight="220"
                 ExtendsContentIntoTitleBar="True"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 Icon="/Assets/AppIcon.ico"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="DialogTitleBar"
                     Grid.Row="0"
                     ShowMaximize="False"
                     ShowMinimize="False" />

        <Grid Grid.Row="1"
              Margin="24,8,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="MessageTextBlock"
                           FontSize="13"
                           TextWrapping="Wrap" />
                <TextBlock Margin="0,14,0,0"
                           FontSize="13">
                    <Hyperlink Click="SetupHyperlinkOnClick">
                        <Run x:Name="SetupLinkRun" />
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Row="1"
                        Margin="0,16,0,0"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <ui:Button MinWidth="156"
                           Margin="0,0,8,0"
                           Padding="14,0"
                           Appearance="Secondary"
                           Content="Open setup page"
                           Click="OpenSetupPageButtonOnClick" />
                <ui:Button MinWidth="92"
                           Padding="14,0"
                           Appearance="Primary"
                           Content="Close"
                           Click="CloseButtonOnClick" />
            </StackPanel>
        </Grid>
    </Grid>
</local:FluentDialogWindow>
